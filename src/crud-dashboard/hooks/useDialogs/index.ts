// Export the hook
export { useDialogs } from './useDialogsHook';

// Export the components and types
export * from './useDialogs';

// Export the provider and context
export { default as DialogsProvider } from './DialogsProvider';
export { default as DialogsContext } from './DialogsContext';
